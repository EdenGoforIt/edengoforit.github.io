# Overview

> Chatgpt is powered by LLM.
> An LLm is trained by reading huge amounts of text and learning how language works.
> An LLM is an AI system designe dto understand and generate human language.

# Machine Learning

> Rule are written by humans and patterns are made by AI.
> A Transformer is a neural network architecture that lets AI understand relationships between words using attention. It breaks text into tokens, looks at all tokens at once, learns which ones matter, and then predicts the next token.

# Deep Learning

> Deep learning is a method of training neural networks with many stacked layers, where each layer transforms the data into increasingly abstract features so the model can automatically learn patterns and make predictions such as the next token.

# Neural Network

> A neural network is just multiple layers chained together, where each layer transforms the output of the previous one:

# Embedding

> An embedding ia nuemerical represetnation of the text.
> A token is a small piece of the text; toekn can be a whoel word, part of a word or a short chracter sequence.
> Once the text is split into tokens, each token is mapped to a unique number
